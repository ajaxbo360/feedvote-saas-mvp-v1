name: test
on:
  pull_request:
    branches:
      - main
      - staging
  workflow_dispatch:

jobs:
  redirect:
    runs-on: ubuntu-latest
    steps:
      - name: Redirect to CI workflow
        run: |
          echo "This workflow has been replaced by the ci.yml workflow."
          echo "Please use the CI workflow instead."
          exit 1
